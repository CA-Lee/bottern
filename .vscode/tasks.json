{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bottern: run server",
            "type": "shell",
            "command": "uvicorn",
            "args": [
                "--host=0.0.0.0",
                "--reload",
                "app.main:api"
            ],
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}